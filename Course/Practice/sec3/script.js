// Use strict mode
"use strict";

let hadDriversLicense = false;
const pastTest = true;

if (pastTest) hadDriversLicense = true;
if (hadDriversLicense) console.log("I can drive!");

const sounding = "Audio";

console.log(sounding.length);
