/* 
- In this file, we catch the user's input, storing it into a variable.
- Always convert to Number if the user input was a number data type.
*/

const myAge = Number(prompt("What is your age? "));
console.log(`My age is ${myAge}`);
